import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// This will tell react which component to start rendering and where in the html file to populate
ReactDOM.render(
    <App />,
  document.getElementById('root')
);
